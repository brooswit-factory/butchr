{
  "name": "@brooswit/butchr",
  "version": "0.1.0",
  "description": "The software factory: watches your Jira, runs herdr agents on active tickets, pushes updates to them over MCP",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/daemon/index.ts",
    "generate": "bun run scripts/load/generate.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "test:unit": "bun test test/unit test/load && bun run scripts/coverage/gate.ts",
    "release:check": "bun run scripts/release/check.ts",
    "check": "bun run generate && bun run typecheck && bun test test/unit test/load && bun run scripts/coverage/gate.ts && bun run scripts/verify-generated-is-committed.ts"
  },
  "dependencies": {
    "@brooswit/thatch": "^0.6.0",
    "@brooswit/herdr-sdk": "^0.1.2",
    "@brooswit/sundry": "^0.2.1",
    "elysia": "^1.4.29"
  },
  "devDependencies": { "@types/bun": "^1.2.0", "typescript": "^5.6.0" },
  "engines": { "bun": ">=1.1" }
}
