bump: patch

### Fixed
- **Two false claims in BUTCHR-353's own doc comments, caught at review, corrected (no behaviour change).** `gatherWorkerSignals`'s doc comment said a failed `labels()`/`comments()` read for a worker "simply DROPPED" that worker from every wake signal; it actually drops only the one signal whose read failed (`inReview` is read off `WorkerLink.status` alone, unaffected by either read). Separately, the 20-comment ask-window's stated reason for not being fixed ("would disturb the cost bound") was wrong — reusing `getIssue`'s already-paid-for embedded comment block would LOWER the cost bound, not disturb it; the real reason is that block is ascending/oldest-first with an unconfirmed cap, a pattern this codebase already tried and reversed elsewhere (`src/tools/defs.ts`). Also reworded the "correctly waiting" wake's tail sentence, which read as though the wake comment itself clears `agent:stalled` — it never does, since it starts with `DAEMON_CHATTER_PREFIX` — to say explicitly that a reply from the agent counts and the wake does not.
