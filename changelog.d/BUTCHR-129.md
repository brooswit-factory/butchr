bump: patch

### Fixed
- **The frozen-asleep detector's project-tier dedupe-by-adoption never actually matched, so a daemon restart during a live freeze re-posted the `[butchr:frozen]` complaint (BUTCHR-95/123/129).** `commentsOnOwnChannel` (`src/daemon/index.ts`, BUTCHR-123) read a project's own Confluence root-doc comments back raw (`body: r.body`), but `ops.getPageComments` returns storage-format XHTML — the same `<p>${escapeStorageText(text)}</p>` wrapping `speakOnOwnChannel` writes, not the plain text that was posted. `findMarked`'s `startsWith(marker)` anchor never matches a wrapped body, so the detector could never adopt its own prior complaint on the ONLY resource type `atRest` (and so this detector) can ever apply to — every restart re-posted, bounded only by the in-memory rate cap, which the same restart also clears. Rule 2b's third form: right resource, right call, wrong representation — passed two independent reviews.
- Fixed by deleting `commentsOnOwnChannel` and pointing the detector's `comments` dependency at `ownChannelComments` (BUTCHR-124, PR #184) instead, which was already correct (`body: unwrapStorageParagraph(r.body)`) but previously wired only into the blocked-dialog escalator. One project-aware comment reader now serves both callers, rather than two near-identical ones free to drift.
