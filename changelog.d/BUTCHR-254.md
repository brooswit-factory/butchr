bump: minor

### Added
- **BUTCHR-222/BUTCHR-224's blind-spot witness mechanism now covers `src/media/family-scan.ts` and `src/media/media-scan.ts`, the two detectors the sibling story deliberately left out.** `FAMILY_BLIND_SPOTS` (family-scan.ts, 6 entries, all witnessed) and `MEDIA_SCAN_BLIND_SPOTS` (media-scan.ts, 2 entries, 1 witnessed and 1 `witness: null` with a written reason) replace both files' prose "WHAT THIS SCANNER CANNOT SEE" sections. Both files' module headers now point at their own list instead of re-enumerating the claims.
- **`family-scan.ts`'s prior-art pair (a real silence assertion sitting next to, but not structurally paired with, its positive control) was folded into a single `witnessBlindSpot` call** — the two previously-separate tests ("THE SWEEP_JQL SHAPE" / "THE DERIVED FORM") are now one witness, making the positive control load-bearing rather than adjacent-and-deletable.
- **`noValueLevelAnchor` (family-scan's "a family with no value-level anchor at all" blind spot) is witnessed, not left as a scope-fence gap** — its witness demonstrates the blindness itself using an ad hoc `Family` object built only inside the test, and explicitly does NOT invent a real anchor for `pr:*`, `PARENT`, or `{{GROUND_TRUTH}}`'s own ungraded sub-record gap in production code.
- **`media-scan.ts`'s blind spot was SPLIT INTO TWO ENTRIES after review refuted the `noWitnessReason` the single entry carried** — and the split is the substantive change, not a rename. `nonRegistryConventionMediumInvisible` states the MECHANICAL claim (a medium with no `registry.ts` **inside the scanned root** is invisible to this scan by construction, because it lists directories one level under `srcDir` and tests each for `registry.ts`) and is now **witnessed**, with a positive control: a sibling medium in the same fixture root that does have a `registry.ts` IS found. `docTitleHandDeclarationCurrency` keeps `witness: null` for the CURRENCY claim (that the hand-maintained set of structurally-enforced media in `MEDIA_REGISTRY` is complete and current), whose falsifying input is a medium **nobody wrote down** — an absence in a human's attention, which no fixture can construct. Bundled, the unwitnessable half's reason was excusing the witnessable half; nothing in the mechanism could see that, because **a `noWitnessReason` is prose and nothing checks whether it is true.** That gap is now named in `media-scan.ts`'s own header, beside the per-list coverage residue `blind-spot.ts` already names.
- **`MEDIA_REGISTRY`'s per-medium `blindSpots` field (`src/media/registry.ts`) is no longer a free-standing, independently-driftable prose copy of each detector's own blind-spot claims.** A new `blindSpotIds` field ties each of `labels`/`headers`/`workspace` directly to that medium's own scanner-file `*_BLIND_SPOT_IDS` export **by import, not by copying values** — the same array object, so there is no second copy left to drift (proven in `test/unit/media-registry.test.ts` via `.toBe` reference-equality, not `.toEqual`). `blindSpots` itself is trimmed to a short pointer plus whatever is genuinely unique to registry.ts's own grading analysis (e.g. `workspace`'s `PARENT`/`{{GROUND_TRUTH}}` ungraded-gap discussion, which is not a scanner claim at all). `docTitle` has no scanner module and therefore no id list to tie to — `blindSpotIds: null` with a written `noBlindSpotIdsReason` is the honest value there, argued rather than silently left, and deliberately distinct from `media-scan.ts`'s own `MEDIA_SCAN_BLIND_SPOTS` reasons (those are about media-scan's blindness to a structurally-enforced medium's shape and about whether the hand-maintained set of such media is current; this one is about `docTitle` having no list of its own gaps to reference).
- New type-level door: `BlindSpotIdsField` (`{ blindSpotIds: string[] } | { blindSpotIds: null; noBlindSpotIdsReason: string }`), same "you cannot say none without writing why" discipline `DetectorField` already uses — proven, not merely asserted, via `@ts-expect-error` in `test/unit/media-registry.test.ts`.

### Fixed
- **A vacuous assertion in `test/unit/media-scan.test.ts` that read as a silence check and could not fail in the direction that mattered.** `expect(modules.every((m) => m.endsWith("/registry.ts"))).toBe(true)` is TRUE on an empty array (measured: `[].every(...)` → `true`), so it passed against a dead `listRegistryModules`. It is now anchored to a non-empty result first (`expect(modules.length).toBeGreaterThan(0)`, then `expect(modules.filter((m) => !m.endsWith("/registry.ts"))).toEqual([])`, which is `false` and `[]` respectively on an empty array). **Stated precisely, because the narrower claim is the true one:** no verdict changed, since the enclosing test already failed earlier on a `toContain` under the same mutation. What was wrong is that the shape assertion itself could not fail; that is fixed, and the test no longer contains a check whose green is uninformative.
- **A stale block comment above the `MEDIA_SCAN_BLIND_SPOTS` describe block**, which asserted "No `test()` here calls `witnessBlindSpot`" and that the coverage call "passes trivially here since this list declares none". Both were true when written and both became false with the split. The comment now records that it was wrong rather than being silently rewritten — a cached description outliving what it described, this epic's own defect occurring in this epic's own test file, and one nothing in the mechanism would ever have caught: `assertBlindSpotCoverage` grades entries, never the prose around them.

### Verified
Method, so a later reader can reproduce rather than trust: every number below was measured on a clean tree at this branch's own head, after the base-merge of `main` rather than carried across it, with `bun run check` for the gate and per-file `bun test <file>` for the mutations. Each mutation was reverted and `git status` confirmed clean before the next.
- **Detector-killing mutation (criterion 2), `family-scan`: making `findFamilyCollisions` return `[]` unconditionally produced 6 failures at `positiveControl` and 0 at `silence`** — all six `FAMILY_BLIND_SPOTS` witnesses — for 10 fail / 12 pass in that file. That reproduces the sibling story's "N at positiveControl, 0 at silence" shape at double its N, and is the measurement proving the pairing is load-bearing: every silence half passed *trivially* against a dead detector.
- **The same attack on `media-scan` — making `listRegistryModules` return `[]` — now produces 1 failure at `positiveControl` and 0 at `silence`** (9 pass / 5 fail: the one witness at its positive control, the list's `assertBlindSpotCoverage` call going red because the aborted witness never recorded its id, and 3 ordinary non-witness tests). **Before the split this same attack produced 0 and 0**, because the list contained no witnesses at all and its coverage call passed *trivially against a dead detector*. That earlier number is recorded in `media-scan.ts`'s own header rather than dropped: it is the measurement that made the split necessary, and it is what a reader should expect to see return if the witness is ever deleted.
- **Phantom-witness demonstration (criterion 3): pointing a `FAMILY_BLIND_SPOTS` entry, and separately `MEDIA_SCAN_BLIND_SPOTS`'s witnessed entry, at a witness id no test executes turned each list's `assertBlindSpotCoverage` call red**, naming the entry and the unexecuted id. **In both cases `bun run typecheck` exited 0 with the phantom id in place**, which is the half that matters: the type door is structurally blind to a witness id naming a test nobody wrote, and only the runtime coverage call catches it.
- **This was not a hypothetical.** The task agent found a real, unreverted `family:suffix-concatenation-PHANTOM` id left in the predecessor's draft by an abandoned mutation, and `assertBlindSpotCoverage` is what caught it — the inherited residue doing its job, on this increment's own work, before the code ever reached review.
- **`MEDIA_REGISTRY.blindSpotIds`'s reference-equality tie was itself mutation-verified**: swapping the direct import for a spread copy (`[...LABEL_BLIND_SPOT_IDS]`) failed the `.toBe` test (a `.toEqual` would not have caught it); an empty-array mutation failed both the non-empty shape check and the reference check; a non-null mutation on `docTitle`'s `blindSpotIds` failed its own null-check test. All reverted; `git status` clean afterward.
- **Full gate (`bun run check`) green at this head: exit 0, 2151 pass / 0 fail across 173 files; typecheck clean; coverage gate passed against its 90% minimum; build succeeds.** No coverage percentage is pinned here on purpose: review measured a different covered-line count than this branch's author did at the same sha, three consecutive runs each, same denominator — the suite's documented wall-clock sensitivity (`docs/expect-tally-non-determinism.md`) showing up in the covered-line count and not only in the `expect()` tally. A pinned percentage is a number a later reader will try to reproduce and fail to, which is the same shape as the stale figure this fragment already had to correct once. (An earlier draft claimed `1883 pass / 95.35%` — numbers inherited from a predecessor's tree that no longer described this one.)
- **One intermittent failure was observed and is not this change's:** the first gate run of this head failed 1 test, `test/unit/sleep.test.ts` ("a sleeping resource … produces zero spawn/stop/respawn across many polls"), `Expected: > 1, Received: 1` — a real-timer polling assertion in a file this branch does not touch, matching BUTCHR-168's documented ~1-in-10-to-20 intermittent. The immediately following run was 2151 pass / 0 fail. Reported rather than re-run into silence.

### Honest limit, carried forward
**Tying `blindSpotIds` to the scanner's own array closes drift between two copies — it does not make the enumeration complete, and it does not make either end of it true.** The registry and the scanner can no longer disagree about what the list says, because there is only one list; the list itself is still self-declared. `blindSpots` beside it is still unchecked prose, narrower than it was but not verified. This qualifier is stated in `src/media/registry.ts`, `src/media/family-scan.ts` and `src/media/media-scan.ts` themselves, not only in this fragment, because a module that looks exhaustive is how the overclaim actually gets made.

**And a `noWitnessReason` is still only an argument.** `MEDIA_SCAN_BLIND_SPOTS.docTitleHandDeclarationCurrency` remains argued, never verified; a green `assertBlindSpotCoverage` says nothing about it beyond "it declares no witness id". This increment is itself the evidence that the distinction is not academic: one such reason shipped, passed both doors, and was wrong — caught by a reader who tried to build the witness anyway, which is not a mechanism.

This closes "is the *stated* blind spot true," never "is the *list* of stated blind spots complete" — nothing here checks that every prose bullet became an entry, or that either detector has a blind spot its author never noticed. That residual layer stays self-declaring; see `src/media/blind-spot.ts`'s own header for the mechanism's stated limit, which this ticket does not remove. The per-list `assertBlindSpotCoverage` gap (deleting a coverage call silently loses the runtime door while the type door survives) is inherited unchanged and deliberately left unguarded — see that same file's header for why a fourth guard checking the guards is refused on the same grounds as `PARENT`/`{{GROUND_TRUTH}}`.
