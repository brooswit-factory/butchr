bump: patch

### Added
- **`docs/windows-wsl-host.md` gains a "Coexisting with another supervisor (e.g. usrr) on the same host" section (FACTORY-80, implements FACTORY-62, story of epic FACTORY-58).** Docs-only: an ownership-boundaries table (which systemd units/Windows scheduled task Butchr's own install owns vs. what a different supervisor owns), what to check to avoid the two stepping on each other (scheduled-task name collisions and why `-TaskName` exists, the shared WSL VM lifetime, port/resource considerations including two verified footguns — a `BUTCHR_PORT` change needing a matching `cli.ts verify --port`, and herdr's `LimitNOFILE` drop-in only covering `herdr.service` — and no shared credential files), how to read the existing three-state health check as an elimination step for which side a problem is on, and an illustrative, explicitly secondhand worked example (zippy: `Butchr-WSL-Zippy` alongside an untouched `USRR-Zippy` task). No script, behavior, or Linux-host change.
