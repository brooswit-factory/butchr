bump: patch

### Fixed
- **The `createDashboardFeed` test "repeated `snapshot()` reads never call `list()` again and never change `confirmedAt`" now actually exercises that claim (BUTCHR-327).** It built its feed with a frozen clock (`feed(() => 1000)`), so an implementation that re-stamped `confirmedAt` with `deps.now()` on every read would still write back the identical value and pass unnoticed — a `snapshot()` that never advances `confirmedAt` on a read and one that does were indistinguishable under a clock that never moves. The clock now moves between the poll and the reads (`now = 9000` before `f.snapshot()` is first called); the test's existing assertion that `confirmedAt` still reads `1000` after that jump is what makes the two implementations distinguishable again. No production code changed. Verified directly against a re-stamp-on-read mutation of `createDashboardFeed`'s `snapshot()`: it now fails `repeated snapshot() reads never call list() again and never change confirmedAt` (was 2288 pass / 0 fail — silent survival — before this fix).
