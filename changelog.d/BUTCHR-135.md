bump: patch

### Fixed
- **The stale-approval protocol text across every agent-facing instruction channel now states what the last-decisive-review check actually does and doesn't cover, per BUTCHR-138's measurement (`docs/review-commit-immutability.md`), instead of the narrower BUTCHR-74 picture it used to teach.** `briefs/task.md`, `briefs/story.md`, `briefs/epic.md`, `docs/agent-model.md`, and `src/agents/pr-nudge.ts` (doc comment and rendered nudge string) now say: a mismatch against `reviews[].commit.oid` is a real signal the branch's own contribution changed since that review, while a match is the narrower claim that it did not — not a guarantee that nothing unreviewed is in what merges, since a clean base-merge imports `main`'s own, separately-reviewed content. They name the propagation race (`reviews[].commit.oid` was observed to take ~30-56s to catch up after a base-merge — an OBSERVED window, not a guaranteed bound — so a read taken immediately after a push cannot be trusted as final). They name what remains untested (squash-merge, rebase, force-push, a merge's second parent, a genuine multi-way conflict resolution) rather than treating the check as covering them. And they carry the transcription caveat on the append-only `[review] APPROVED ... @ <sha>` Jira line: unrewritable is not the same property as error-free, since a hand-typed sha has already gone out wrong (39 characters, naming no real commit) in this protocol's own measurement — trust that line only when the sha was pasted verbatim from `gh pr view --json headRefOid`, never retyped, and check it yourself for exactly 40 hex characters before acting on it. No channel implies any structured API surface (REST-list, REST-by-id, or GraphQL) is immutable, and none proposes reading REST instead of GraphQL as a remedy. `test/unit/merge-check-guard.test.ts`'s own BUTCHR-74 comment is updated to match the newer measurement; its assertions are unchanged and stay green. Prose only — no change to `src/agents/pr-nudge.ts`'s runtime behavior or to what the daemon does or when.
