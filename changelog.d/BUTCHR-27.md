bump: patch

### Added
- **A freshly created ticket doc now points its agent at the ASSIST space, which nothing previously routed anyone to (BUTCHR-27).** The assistant documents this estate — how work is created, routed and reviewed, how the fleet is run, and where it has failed before — in a Confluence space that no brief, ticket or tool linked, so in practice it was written and never read. The operator's reason for closing that gap is the same premise the doc-per-ticket work rests on: knowledge that exists but is never read is not knowledge, and a library nobody is sent to is a diary. The pointer goes in `provisionalBody()` because that paragraph is the only text the tool itself ever authors and the one thing a newly-born agent is certain to read — it links the space plus two durable entry points (the end-to-end factory walkthrough and the assistant/agent working agreements) rather than an enumeration, since a list of pages hard-coded in a source file goes stale silently and this is the last file anyone would look in to fix it. The two cited pages were each read before being cited: the cold-start page, the obvious-looking pick, is written for the assistant operating the fleet rather than for a worker agent on a ticket. The pointer is transient by design — the first `set_doc` replaces the whole body, pointer included — which is correct rather than a leak, because by then the agent has read it and the doc's job has changed from orienting its author to recording what happened.
