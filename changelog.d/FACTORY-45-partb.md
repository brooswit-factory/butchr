bump: minor

### Added
- **Butchr now pins drovr's own host-neutral escalation hook, catching unrecognized dialogs Butchr's own parser might miss (FACTORY-45 Part B).** `@brooswit/drovr` bumped to 0.15.0. A new, independent poll loop calls that release's `createBlockingEscalationWatcher(...).poll(herdr)` every 5s over the whole fleet; its `onUnknownDialog`/`onDialogResolved` hook feeds the SAME minimal managed-session escalation Part A shipped (the `[managed-escalation]` journal line + `/health`'s `managedSessionEscalations` mark) whenever the pane resolves as a managed session — a keyed pane, or a non-managed keyless pane, is a no-op there, leaving Butchr's existing detection/escalation pipeline fully in charge of both, unchanged. Butchr's own dialog-answering (`src/agents/prompt.ts`) is left untouched: it still independently handles several dialogs drovr's own release does not (resume-from-summary, settings-warning/recommendation, Bypass-Permissions) — see `docs/managed-sessions.md`'s "Two detectors, one mark" for the deliberate, documented overlap this leaves in place for the dialogs both now recognize.
