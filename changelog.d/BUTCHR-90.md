bump: patch

### Fixed
- **`docs/agent-model.md`'s agent-type table said Epic agents run on `fable`; they run on `opus`, and now the doc agrees.** This corrects a stale table cell, not a design decision made today: no human ever confirmed which model Epics should run on when asked — twice, with `[ask]` markers, over several hours (BUTCHR-53). `opus` is only the completion of a decision already made and recorded in commit `ca38dd15` / `CHANGELOG.md`'s `## [0.15.0]` entry (2026-08-29): Fable capacity was exhausted fleet-wide, so `modelFor()` moved Epics to `opus` while the doc's own build-order line (100 lines below the table, same file) was updated to match and the table cell was missed. That single change touched five files and updated one of the two places `docs/agent-model.md` states the Epic model, missing the other, in the same file it already had open — and a fully green suite did not catch it for three days, because nothing compared the two. `CHANGELOG.md`'s `## [0.15.0]` entry names an explicit revert condition, still live and unresolved: **"revert the map when Fable capacity returns."** Whether Fable capacity has since returned was not established by this change and is not a repo-inspectable fact; anyone who learns that it has should treat this fix as the one to undo. `docs/` is not release-gated and this fix, on its own, would not require a fragment — this one is written anyway to keep the above record attached to the change that completed it, alongside the drift guard below.

### Added
- **A new guard, `test/unit/agent-model-guard.test.ts`, asserts `docs/agent-model.md`'s agent-type-table Epic cell agrees with `modelFor("Epic")`**, closing the one gap BUTCHR-89's channel sweep found unguarded (`test/unit/merge-check-guard.test.ts`'s existing drift guard, BUTCHR-56/BUTCHR-74, covers a different fact — the merge-check instructions — across a different, wider channel set, and deliberately does not extend to this one). Bounded to this single pair on purpose, not a general facts-checker: it does not cover the Story/Task/Project rows, the doc's build-order section, or any other channel. It asserts AGREEMENT, derived from both sides at test time (the doc cell is parsed out of the table row; `modelFor("Epic")` is called directly), not a hard-coded literal — the next legitimate change to the fleet's epic model will not need to hand-edit this test to stay green, only to update both sides together. Watched go both ways: run against the pre-fix tree (doc `fable`, code `opus`) it failed with `Expected: "opus", Received: "fable"`; after the one-line doc fix above it passed, 2 pass / 0 fail. The assertion site carries a FORWARDING ADDRESS comment naming the still-open Fable-capacity revert condition and stating explicitly that a deliberate future change to the intended value should turn this test red until both sides are updated — and that the fix for that red is to update the stale side, never to delete the assertion.
