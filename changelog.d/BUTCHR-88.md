bump: patch

### Fixed
- **`correct_worker`'s agent-facing text no longer claims that no agent could ever correct an epic's own description — that claim stopped being true the moment BUTCHR-62 shipped the project tier, and the text kept asserting it anyway.** A project caller could already correct one of its own epics' descriptions — `assertOwnWorker` accepts a project caller for a target that is both a member of its own project and an Epic, and `correct_worker` never calls `refuseProjectCaller` — the documentation simply never caught up. Fixed at every site that carried the claim: the `correct_worker` tool description and its doc comment in `src/`, the `correct_worker` paragraph in `briefs/epic.md`, a new paragraph documenting the verb for a project caller in `briefs/project.md` (which carried no mention of it at all), the still-unreleased `changelog.d/BUTCHR-60.md` fragment, and the guard test in `test/unit/tools.test.ts` (which previously pinned the false sentence in place and now pins the corrected one, with the retracted phrasing asserted absent). Text-only: no behaviour changed, and the unconditional self-refusal — nobody, at any tier, can correct their own ticket — is unaffected and still the named recourse for a person to edit directly in the Jira UI where no accepted caller exists.
