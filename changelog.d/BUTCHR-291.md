bump: patch

### Fixed
- **`abandoned.ts`'s module-level doc comment on why `butchr:shelved` is deliberately not exempted no longer contradicts the code it describes (BUTCHR-291).** BUTCHR-240 fed the abandoned detector a second JQL search (`TODO_WORKER_JQL`) so To Do workers enter its candidate set — but the pre-existing paragraph explaining the no-exemption decision still told the reader the opposite: that a shelved worker (moved to To Do by `shelve_worker`'s own contract) was "outside this detector's candidate set entirely" and that the decision only ever bit on an anomalous case. That conclusion is false on this branch: a shelved To Do worker under a Done boss is now an ordinary candidate, reached because the input layer changed, not the predicate. The paragraph is rewritten to say so, and its dangling cross-reference (`KNOWN LIMITATION`, a heading BUTCHR-240 renamed) now points at the heading that actually exists (`FORMER KNOWN LIMITATION`). The no-exemption decision itself, and the `IssueLink`-never-hydrates-`labels` sentence, are unchanged. Comment-only — no behaviour, no test change.
