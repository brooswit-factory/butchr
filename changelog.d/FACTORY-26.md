bump: patch

### Fixed
- **`filesystem`'s capability cells re-truthed after BUTCHR-407 landed a real filesystem query provider on `main` (FACTORY-26, implementing FACTORY-21).** `capabilities.ts`'s MATRIX now declares `query: true` for `filesystem`: `listFilesystemResources` (`src/resources/filesystem.ts`) is a real, generic query entry point — given a structured `FilesystemQuery`, it walks the tree and returns every matching resource, the same query→results shape as `github-issue`'s `searchAll`/`jira-work-item`'s `search`. `read` and `snapshot` were re-verified against the current code and stay `false`: `FilesystemResource` carries no content, and the one content-reading code path (`readDefinitionFile`, `src/rules/session-definition-type.ts`) is internal to that one rule type, not a generic `read(ref)`; the real (kind, size, mtime) poll/diff wiring `filesystem-type.ts` now has is the daemon's own internal per-provider agent-lifecycle plumbing, not a shared, generically-invokable snapshot entry point, matching `github-issue`'s/`zendesk-ticket`'s own already-`false` `snapshot` cells for the same reason.
- `docs/provider-capabilities.md`'s filesystem row and prose are updated in place with file:function evidence; the pinned `EXPECTED_MATRIX` in `test/unit/capabilities.test.ts` and its per-provider filesystem test are updated to match. Also confirmed and noted: BUTCHR-408's `session-definition` is a rule type built over `filesystem` resources, not a ninth capability provider.
