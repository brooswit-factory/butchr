bump: patch

### Fixed
- **`briefs/project.md`'s "Name your inbound surfaces precisely" paragraph named a wake writer that cannot exist and omitted a real wake axis.** It claimed root-doc comments come "from any caller's `report_to_boss`/`ask_boss`" — false: `src/tools/speak.ts`'s single project-id branch is the only path to a root-doc comment, so an ISSUE caller (Epic, Story, or Task) can never write one; only the project's own `report_to_boss`/`ask_boss`, a peer's `tell_peer`, or a direct comment can. It also said there are "exactly two" inbound surfaces, "full stop" — the project's activation verdict (`src/resources/project.ts`) actually ORs three axes (root-doc version/body edits, root-doc comments, and In-Review epics' ticket comments), and the version axis is real and in use. The brief now names all three axes and the real comment writers, while keeping its conclusion that an In Progress epic reaches the project through none of them.
- **"escalate to a human" named no mechanism, in `briefs/epic.md` and the escalation loop's blocked-dialog follow-up (`src/agents/escalation-loop.ts`) — no verb reaches a human.** Both now say the concrete substance instead: still call `report_to_boss`/comment on your own ticket; only a person reading that ticket directly will ever see it; no agent will answer; don't wait as though one will. The escalation-loop follow-up stays tier-neutral (no issue-type fetch is available at that call site).
- **`report_to_boss`/`ask_boss`'s tool descriptions (`src/tools/defs.ts`) claimed DELIVERY for a Story/Task caller** ("always reaches its boss" / "always eventually found") when the wiring only guarantees the comment lands on a surface the boss watches — the boss itself can still be withheld at the agent cap or blocked by quota (measured on BUTCHR-318 itself). Both now state the true, narrower claim while keeping the Story/Task-vs-Epic contrast sharp.

No verb, wake edge, predicate, or route changed — words only, correcting defects BUTCHR-277 found reviewing BUTCHR-318's own words-only fix.
