bump: patch

### Fixed
- **`briefs/project.md` claimed "an epic has no verb that edits or comments on your root doc" — the "edits" half was false.** `confluence_update_page` (`src/tools/defs.ts`) is an unguarded, general-purpose full-body-replace with no check on whose page it is, so an In Progress epic CAN overwrite a project's root doc body with it — which would bump the page version and could wake the project through the VERSION axis. Only the "comments" half was ever true (the comment-on-page op (`commentOnPage`) has exactly two call sites, both project-caller-only). The brief now says precisely that an In Progress epic has no relationship verb that reaches a project at all and no verb that comments on its root doc, names the unguarded edit path, and states plainly that it is a destructive full-body replace of the project's living brief, not a message channel — an epic still cannot reach its project as a correspondent while In Progress, but that conclusion now rests on the true facts.
- **`changelog.d/BUTCHR-318.md` had gone stale against what BUTCHR-331 actually shipped in the same release.** It said `briefs/epic.md` "tells an In Progress epic ... to escalate to a human immediately" — BUTCHR-331 replaced that framing, since "escalate to a human" named no mechanism; the shipped text instead tells the epic to call `report_to_boss` anyway, plainly stating only a person reading the ticket directly will ever see it. It also said `briefs/project.md` "names its two inbound surfaces precisely" — BUTCHR-331 corrected that count to three (the root-doc version/body-edit axis was previously omitted). Both bullets are amended to match the text that actually ships, so the release notes no longer contain two Changed/Fixed bullets asserting things that contradict each other.

No verb, wake edge, predicate, guard, or route changed — words only.
