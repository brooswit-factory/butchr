bump: patch

### Fixed
- **`docs/review-commit-immutability.md` reconciled against BUTCHR-156's contradicting measurement (BUTCHR-181).** BUTCHR-156's closing doc reported `reviews[].commit_id` "rewritten forward to the current head for every review" and that the staleness check built on it "cannot fail." Both generalisations are false: this repo's own merged PR #172 (already in the document) has two reviews that still hold distinct historical shas, and BUTCHR-156's own evidence (real PRs #218/#219, independently re-verified here with `git merge-tree --write-tree`) turns out to be exactly the chain-intact-clean-merge case this document's Arm A/Arm C already predicted moves the field — corroboration, not a counter-example. The document now states this reconciliation explicitly, keeps the underlying property unchanged, adds a new finding (mandatory branch-protection base-merges can make the manual `[review]`-line comparison show a false staleness signal), and states its own limited reach: this correction does not reach an agent whose brief is served from a stale daemon tree or already on disk (BUTCHR-150's gap, not closed here). Three new read-only snapshots (PR #172, #218, #219) committed to `docs/review-commit-immutability/snapshots/` alongside the original 18. No file in the repository needed correcting for this — `briefs/story.md`, `briefs/task.md`, `briefs/epic.md`, and the docs that reference this check already carry the sound text on `main`, verified by the existing mutation-tested `merge-check-guard` suite (60/60 green). **That is a claim about `main`, not about what a running agent is served**: an EARLIER correction to this same protocol (BUTCHR-148, `main` commit `09cbceb`) removed a different refuted claim from these same two brief files, and a live daemon was found, on 2026-09-02, still serving both files with that removed text intact from a checked-out tree well behind `main` — measured on the reviewing agent's own host and confirmed a second time by that agent's own running `brief.md`. Landing a correction on `main` is necessary and is not sufficient on its own; the deploy/brief-caching gap that explains the difference is BUTCHR-150's, not this ticket's, and is not closed here.
