bump: patch

### Fixed
- **`test/unit/merge-check-guard.test.ts` now also covers `briefs/epic.md` and `briefs/story.md`'s reviewer-side `[review]`-line instruction (BUTCHR-165), extending BUTCHR-149's `briefs/project.md`-only coverage to the two remaining reviewer channels.** Both briefs were already built and read by the guard (`briefChannels()` derives its issue types from `knownBriefTypes()`), so only the positive assertion was missing — the text could be deleted and the suite stayed green.
- **Measured, not assumed: `briefs/epic.md` and `briefs/story.md` write the sha placeholder as `@ <full 40-char sha>`, while `briefs/project.md` writes `@ <sha>`** — two different, both-correct renderings of the same instruction. `REVIEW_LINE_APPROVED`/`REVIEW_LINE_CHANGES_REQUESTED` now match an alternation of exactly those two known-shipped literal strings, not a general wildcard, so every channel stays exactly as strict as before while also accepting the second real form.
- **`briefs/task.md` is deliberately excluded, not silently skipped:** its `[review] APPROVED ... @ <sha>` mentions are author-side text a task reads when it wakes, never an instruction to emit a `[review]` line (a task has no worker below it to review). Recorded in `EXCLUSIONS` with that reason.
- **Found and fixed a vacuity hole in the transcription-caveat check while mutation-testing `briefs/story.md`:** checking `TRANSCRIPTION_CAVEAT` against the whole channel text was silently vacuous for that one channel, because `briefs/story.md` carries the "pasted verbatim" / "retyped by hand" phrase pair a second, unrelated time in its own author-side reading instructions (~4.6KB further down) — deleting the actual reviewer-side caveat left the assertion green. The check is now scoped to a window anchored at the reviewer-side `[review] APPROVED` line, which excludes the unrelated distant occurrence.
- Mutation-verified for both `briefs/epic.md` and `briefs/story.md`: deleting the APPROVED clause, the CHANGES_REQUESTED clause, or the (correctly-scoped) transcription-caveat sentence, independently, turns the corresponding assertion red by name; restoring it turns the suite green again. Also mutation-verified the non-vacuity guard (`expect(briefs.length).toBe(REVIEW_LINE_INSTRUCTING_BRIEFS.length)`): a typo'd label that matches no channel turns it red (`Expected: 3, Received: 2`).
