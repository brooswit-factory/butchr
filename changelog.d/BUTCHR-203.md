bump: patch

### Fixed
- **`src/media/registry.ts`'s `workspace` entry now names a sub-record gap `family-scan.ts` already predicted it could never see (BUTCHR-203/BUTCHR-154).** `WorkspacePlaceholder`'s type-level door forces every `{{PLACEHOLDER}}` to have a registry entry, and `GROUND_TRUTH` is correctly registered `self-declaring` — but nothing whatever grades the individual assertions `groundTruthText` (`src/agents/ground-truth.ts`) writes inside that placeholder's own expansion (host, port, systemd unit, journalctl command, daemon pid, build identity/currency). The `GROUND_TRUTH` entry's mechanism prose is a hand-written description of what that function writes, and the only test touching it (`test/unit/workspace-registry.test.ts`) asserts a substring match against the entry's own reason text, never against `groundTruthText`'s actual output — re-verified in this checkout, not inherited from the ticket. The new `blindSpots` prose states the durable claim (two assertions inside one record, at one instant, can have opposite truth values — grading a record as a single unit is lossy), a dated illustration marked explicitly as expected to be superseded, the three-step re-measurement method for a reader's own workspace, and why no scanner can close this (`family-scan.ts`'s own third named blind spot: a family with no value-level anchor has nothing for a scanner to check membership against).
- No mechanism, scanner, test, or type change added — prose only, matching this file's existing `PARENT` precedent for a live, reachable, ungraded gap.
