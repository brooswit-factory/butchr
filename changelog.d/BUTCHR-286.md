bump: patch

### Fixed
- **Four doc comments describing `StalledTracker`'s removed `streakBroken` latch and its old "idle since spawn" floor now describe what the code actually does since BUTCHR-279 (BUTCHR-272/BUTCHR-286).** `IdleDialogTracker`'s class doc (`src/agents/idle-dialog.ts`) drew an explicit contrast with `StalledTracker`'s `streakBroken` latch and claimed `IdleDialogTracker` was unique in having no "has ever worked" concept — both halves went false the moment BUTCHR-279 deleted `streakBroken` and made `StalledTracker`'s own floor reset and re-arm the same way; the comment now says the two trackers are consistent rather than contrasting them. `src/agents/stall-remediation.ts` had two comments describing `stalled.ts`'s measurement as "the true idle-since-spawn floor" / "the idle-since-spawn duration" — both now say "current-streak" instead, while keeping intact the distinction each comment actually draws (this module's own first-observed-the-label floor vs. `stalled.ts`'s longer, earlier tracker number). `src/labels/sync.ts` and `src/daemon/index.ts` each carried a `KAN-804/807` one-liner describing the stalled signal as "idle since spawn, never spoke"; the first half is now "idle since it stopped working" and the second half ("never spoke") is untouched, since it was already correct. No behaviour changed — every edit is inside a comment block, and `bun run check` reports the same pass/fail/test counts as before this change.
