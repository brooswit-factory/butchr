bump: patch

### Fixed
- **`start_worker` is now taught in `epic.md` and `story.md`, right where `shelve_worker` already is.** It was absent from every brief despite being the only route to reactivate a shelved worker or to pull a worker back from In Review — a hole that would otherwise have been filled by reaching for the deprecated `jira_transition`, the exact generic BUTCHR-38's rewrite exists to retire.
- **The five permanently-retained tool names — `jira_get_issue`, `jira_search`, `jira_add_comment`, `confluence_search_pages`, `confluence_list_spaces` — are now stated explicitly as permanent** (never deprecated, on no removal clock) in every brief where each is used, instead of appearing only as prose mentions or warnings about what a generic *can't* do. `jira_add_comment` is now taught by name, in `epic.md` and `story.md`, as the deliberate SIDEWAYS peer-to-peer channel — the one direction butchr's up/down hierarchy and `tell_worker`'s stranger-key refusal don't cover (two epics resolving a shared boundary, two stories sequencing a file they both touch, a story asking a peer for a contract). `confluence_search_pages`/`confluence_list_spaces` are now named in `epic.md` and `story.md` as permanent, space-wide doc-discovery tools, separate from `get_doc`/`set_doc`.
- **`ask_boss` is now named in `epic.md`** at the exact spot that already told an epic agent to comment and stop when its own description is too vague to decompose — that instruction was already describing an `ask_boss` in every way but name.
- `test/unit/workspace.test.ts`'s existing per-tier relationship-verb guards are extended (not deleted): `start_worker` joins the boss-side verb list for Epic/Story, plus new per-tier guards (never aggregate, per the trap this same ticket's follow-up comment named) for the start_worker/shelve_worker pairing, the Epic-specific `ask_boss` gap, and the five permanent names' per-brief presence.

Follow-up to BUTCHR-38 (PR #116): that rewrite replaced the generic Jira/Confluence verbs with the relationship-verb vocabulary but left these three gaps in an otherwise-approved PR. Purely additive — no brief was restructured, and nothing already-correct was touched.
